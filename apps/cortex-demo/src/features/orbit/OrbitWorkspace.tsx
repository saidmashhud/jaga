import { OrbitScene } from './OrbitScene';

/**
 * Рабочее место — карта дел.
 *
 * Отрисовщик один. Их было два: объёмная сцена как основная и плоская на SVG
 * как запасная для машин без WebGL. Держать две реализации одного и того же
 * оказалось дороже, чем польза от объёма: они расходились — у каждой был свой
 * словарь цветов связей, свой набор возможностей, и починенное в одной
 * оставалось сломанным в другой.
 *
 * Глубина, ради которой сцена и делалась объёмной, никуда не делась: ось
 * внимания переехала из z в расстояние от ядра. Плоская карта говорит то же
 * самое и вдобавок читается читалкой — узлы в ней настоящие кнопки, а не
 * пиксели на канвасе.
 */
export function OrbitWorkspace() {
  return <OrbitScene />;
}
